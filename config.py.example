"""
Application configuration - Template
Copy this to config.py and update with your settings
"""

from pathlib import Path

PROJECT_ROOT = Path(__file__).parent
OUTPUT_DIR = PROJECT_ROOT / "output"
QUESTIONS_DIR = PROJECT_ROOT / "questions"
TEMPLATES_DIR = PROJECT_ROOT / "templates"
ASSETS_DIR = PROJECT_ROOT / "assets"
LOGS_DIR = PROJECT_ROOT / "logs"

WORKBOOK_TITLE = "PostgreSQL Mastery Workbook"
AUTHOR = "Your Name"
VERSION = "1.0"

TITLE_FONT_SIZE = 36
HEADING_FONT_SIZE = 18
QUESTION_FONT_SIZE = 14
ANSWER_FONT_SIZE = 13
EXPLANATION_FONT_SIZE = 12
TIP_FONT_SIZE = 12
INFO_FONT_SIZE = 13

PAGE_MARGIN_TOP = 0.8
PAGE_MARGIN_BOTTOM = 0.8
PAGE_MARGIN_LEFT = 0.8
PAGE_MARGIN_RIGHT = 0.8

SPACE_BEFORE_QUESTION = 6
SPACE_AFTER_QUESTION = 6
SPACE_BEFORE_ANSWER = 4
SPACE_AFTER_ANSWER = 4
SPACE_BEFORE_EXPLANATION = 4
SPACE_AFTER_EXPLANATION = 4
SPACE_BEFORE_TIP = 4
SPACE_AFTER_TIP = 6
SPACE_BEFORE_SEPARATOR = 8
SPACE_AFTER_SEPARATOR = 8

QUESTIONS_PER_PAGE = 5

DATABASE = {
    "dbname": "postgres",
    "user": "postgres",
    "password": "your_password_here",  # CHANGE THIS
    "host": "localhost",
    "port": 5432,
}
